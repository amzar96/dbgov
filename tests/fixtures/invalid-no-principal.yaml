apiVersion: dbgov/v1
kind: AccessPolicy
metadata:
  name: bad-policy
spec:
  principal:
    type: user
  grants:
    - level: table
      schema: public
      tables:
        - foo
      privileges:
        - SELECT
